export type Course = {
  id: number | string;
  code: string;
  name: string;
  credit: number;
  instructor: string;
};

export type CourseDraft = {
  code: string;
  name: string;
  credit: string;
  instructor: string;
};

